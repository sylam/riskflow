/build/
/dist/
/riskflow.egg-info/
/site/
/tmp/
/function_test.py
/.idea/riskflow.iml
/interface.py
/LegacyFVA.py
/conf.py
__pycache__/
*.pyc
tests/_stepper_out*/

# generated docs site (riskflow_docs.py output)
/docs/
/mkdocs.yml

# campaign & run outputs — curated result CSVs are committed explicitly from
# artifacts/walk_forward/ top level; run directories are working data
/artifacts/daily_runs/
/artifacts/backtest/
/artifacts/walk_forward/*/
/artifacts/.~lock.*
# Run outputs, not source. d124947 swept 1461 sweep files (1.5 GB) in because these
# directories had no rule; the curated artifacts/walk_forward top level stays tracked.
/artifacts/sweeps/
/artifacts/lab_runs/
/artifacts/hull_experiments/
/artifacts/hp_logs/
/artifacts/textbook_runs/
/artifacts/hp_*.csv
/artifacts/*.npz*
# Trained checkpoints and measurement output. tb_*.py harnesses ARE source; what they
# print is not (the committed tb_hn_aggregate_bias*.csv study tables are the exception,
# kept as the reference machinery those gates cite).
*.pt
/gates/*.csv

# IDE + notebook noise
/.idea/
.ipynb_checkpoints/
/artifacts/*.csv
/artifacts/walk_forward/
/artifacts/hp_phase*.json
/artifacts/*.png
/lf2_forensics.csv
/artifacts/*.log
/artifacts/*.toy_t30
